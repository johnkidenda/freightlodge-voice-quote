# Optional. The static app works with zero secrets.
# If set while running `npm run dev` / `npm run preview`, the local
# /api/dialog-enhance endpoint can use OpenAI to rewrite the next question.
# Never put a real key in frontend VITE_* vars for GitHub Pages.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# Cartesia TTS (Conversational mode) — public URL of the token/audio proxy.
# The long-lived CARTESIA_API_KEY stays on the proxy only. Never VITE_* it.
# Local Vite serves /api/stt-token (mint) and /api/tts (mp3) when the key is set.
# Production placeholder (replace <ACCOUNT> with the wrangler workers.dev subdomain):
# VITE_STT_TOKEN_URL=https://freightlodge-stt-token.<ACCOUNT>.workers.dev
# VITE_STT_TOKEN_URL=/api/stt-token
VITE_STT_TOKEN_URL=
CARTESIA_API_KEY=

# TypeSafe System One (Jev). Server / proxy only. Never VITE_* this.
# Local Vite serves POST /api/jev when set. The token-proxy stub/worker
# serves POST /jev at the same VITE_STT_TOKEN_URL origin.
TYPESAFE_API_KEY=

# Optional transcript webhook. If unset, Send transcript POSTs silently
# via FormSubmit AJAX to john@freightlodge.com. Mailto is last-resort only.
# VITE_TRANSCRIPT_WEBHOOK_URL=

# Hostinger SMTP — server / token-proxy only. Never VITE_* these.
# Used by POST /email/verify/start, /email/verify/confirm, /email/quote
# (local stub + Vite /api/email/*). Required to actually send mail.
SMTP_HOST=smtp.hostinger.com
SMTP_PORT=465
SMTP_USER=john@freightlodge.com
SMTP_PASS=
MAIL_FROM=john@freightlodge.com

# Local/test only: log a 6-digit code server-side and skip SMTP.
# JSON still never includes the code. Do not enable in production.
# EMAIL_VERIFY_DEV_MODE=1
